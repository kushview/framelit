# Framelit — Copilot Instructions

Canonical guidance lives in [`/CLAUDE.md`](../CLAUDE.md). Read it before making changes.

It covers the project overview, build/run/test commands, coding conventions, architecture, the Qt GUI class-design rules, the shared `Actions` registry, threading contracts, macOS permission handling, and the roadmap. This file is intentionally a pointer so the two never drift.
